<h2><%= question_header(@test) %>:</h2>

<%= render partial: 'form', locals: { test: @test, question: @question,
                                      model: [@test, @question], resource: @question } %>

<%= link_to 'Back', test_path(@test) %>
